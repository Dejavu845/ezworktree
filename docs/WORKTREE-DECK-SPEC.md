# Worktree 工位（全部分支）

检出的 worktree 是人掌控的四资产之一。森林上方用同一套 `WorktreeDeck` 工位，两皮肤共用结构，不第四主视图。甲板只放活树：git 仍挂着、或 `status: active` 且目录还在，并且不是仓根。`archived` 的登记路径和主 checkout 不占工位。掌控四格和侧栏的树数跟甲板同一口径，不把仓根算进去。

## 给人看

每个工位只写四层，两皮肤共用：

1. **任务名**（主标题）
2. **分支**（次，等宽）
3. **路径**（次，可截断；点击复制）
4. **下一手动词**（唯一强调）

HEAD / 短版本号 / 卫生 / 产物版数不进卡片主视线，去检查器「资产清单」。没有自报 %。点卡选中并打开对照，不换主视图。甲板卡上的下一手是文字，不是第二颗彩色主按钮。

## 皮肤

| 皮肤 | 工位 |
|---|---|
| 干练 | 纸面、静止状态点 |
| 工场 | 深色平铺卡，不转、无透视 |

## 不做

第四主视图 · hover 上浮 · 假红绿灯 · 把空分支画成工位 · Three.js · 用户可见文案写 HEAD / sha
